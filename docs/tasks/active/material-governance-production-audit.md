# 素材治理生产影响审计与安全回填准备

- task_id: `material-governance-production-audit`
- status: active
- owner: 产品负责人管理的独立执行窗口
- branch: `codex/material-governance-production-audit`
- base_sha: `21cff91df8b1b564d518acdf40299c6bc2b5c005`
- production_baseline: 启动后必须现场只读核验；文档最近记录为 `25e1ed664b99b9d7ffad99e8aa0103181f3fa7c0`、schema `20260910_01`，不得直接当作当前事实
- task_type: 生产只读审计 + 必要工具实现 + 本地 MySQL 演练 + 隔离 L2/L3；不授权生产写入或部署

## 背景

## 本窗口 change contract（2026-09-11）

- 实际开发基线：`origin/main@6b4c6844086bf0c4e6baac540c3cd28582b9235f`；独立 worktree 与登记分支均已核验干净。
- 类型：生产只读审计、素材身份目录同步工具增强、隔离 MySQL/L2/L3 验证和文档收尾；不改变产品销售语义。
- 最小候选文件：`ai_paths/scripts/sync_material_identities.py`、该工具的直接测试、素材治理相关 current/architecture/contract 证据和本任务文档。若取证证明不需要某类文档变化，则不为凑文件而修改。
- 风险：误连或误写生产、计划生成后目录/数据库漂移、批次中断、死锁、回滚误删已占用别名、审计产物泄露 URL/客户数据。
- 控制：默认 dry-run；MySQL apply/rollback 需要冻结计划、明确目标和二次确认；计划绑定 schema/catalog/manifest checksum；有界批次、重试、可重入进度、受保护 rollback；生产本轮只读。
- 验证：生产现场聚合只读查询；全新 MySQL 8/InnoDB/REPEATABLE-READ 迁移和故障演练；全量确定性测试；虚构身份、隔离数据库、硬阻断发送的 L2/L3。未改前端时不运行无关前端构建。
- 回滚：代码候选仅提交独立分支；工具写入由逐批 undo 逆序回滚，已被 `response_committed` 占用或已漂移的别名拒绝自动回滚。
- ownership：启动时 active INDEX 仅登记本任务，未发现交叉 ownership；禁止触碰 Reply Prompt、主线、B 单、SOP 规则、生产配置、部署和任何生产写入。

统一素材身份、跨来源去重、内容/动作隔离和 `response_committed` 占用已经合入 main。最新生产快照显示，该代码和加法迁移随后随第三方 SOP 修复一起发布，但生产目录/历史是否完成身份同步没有闭环证据；现有同步 CLI 在独立验收时仅明确支持本地 SQLite。

未知身份附件采用失败关闭：无法确认物理身份时图片/视频不发送，文字链路继续。因此当前最大风险不再是代码能否迁移，而是生产素材目录中有多少资产仍无法确认身份、是否已经造成素材交付下降，以及如何在不猜测历史事实的前提下安全完成 MySQL 目录同步和必要回填。

## 任务使命

在一个子窗口中连续完成生产现场只读影响审计、缺失工具补齐、隔离 MySQL 演练和 L2/L3 行为验证，最终给出明确的生产回填 `Go / No-Go`。不要在取证、设计、实现、测试等正常阶段返回母窗口领取下一步；任务范围内发现的工程缺陷应自主修复并复验。

本任务只准备并验证安全回填能力，不执行生产目录同步、历史回填、数据库写入、客户发送或部署。

## 产品目标

1. 明确当前生产是否已经受到素材身份未知的影响，不能只看代码或迁移成功。
2. 建立生产素材目录的真实身份覆盖率、重复组、未知组和业务可用性清单，但不把素材 URL、字节或客户信息写入 Git。
3. 让生产 MySQL 可以通过默认 dry-run、显式 apply、可重入、可中断、可审计和受保护回滚的方式同步 canonical identity；本任务只在隔离环境验证该能力。
4. 证明回填计划不会猜测历史已发送事实、不会跨 `corp_id + wechat + external_userid` 串账，也不会把不同图片、图片与视频错误合并。
5. 证明素材身份治理没有破坏正常文字回复、合法素材交付、合法收款/预约/门店能力和允许的销售性软承接。
6. 输出一次可由后续独立生产任务执行的冻结计划、校验摘要、停止条件和回滚方案。

## 已确认能力边界

- Reply 仍是唯一销售语义决策者；目录、跟进序列、素材和代码不能授予收款、门店、预约完成或写动作。
- 未知身份只关闭媒体附件，不关闭文字沟通和正常销售推进，也不得虚称“已经发图”。
- `response_committed` 只表示 AI 已提交稳定响应并占用素材，不等于平台发送成功或客户送达。
- 没有权威失败事件时不自动释放占用；平台发送失败可能不自动补图是当前已接受限制。
- 感知指纹是保守近似，不是绝对物理同一性证明；视频转码依赖可靠文件 ID 或人工登记。
- 错误别名已经占用后不得自动释放，只能通过独立审计迁移纠正。
- 实际发送闭环及 `response_id/client_message_id ↔ dispatch_id` 映射属于第三方接口任务，不在本任务扩张公共协议。

## 工作范围与自主权

执行窗口应先读取代码和权威合同，自主确定最小实现。允许覆盖：

- 生产版本、schema、相关服务健康和素材身份表的只读核验。
- 外部 Follow Knowledge、版本化配置素材和其他实际候选来源的只读目录审计。
- 最近素材候选、身份判定、响应提交和附件结构的脱敏聚合分析；不得读取或保存完整客户会话。
- 生产同类 MySQL 的目录同步、dry-run、计划冻结、apply、恢复与受保护 rollback 工具；如果现有工具不支持，应在本任务分支补齐并测试，但不得对生产运行 apply。
- 合成/脱敏快照上的迁移、同步、并发、重放、回滚、性能和故障注入。
- 使用虚构身份和隔离数据库的真实模型 L2/L3；必须硬阻断所有发送、生产写入和策略外发。
- 与本任务直接相关的合同、架构、current 状态、任务证据和测试。

精确文件集由执行窗口取证后写入本任务文件。当前无其他 active ownership；仍不得顺手修改 Reply Prompt、销售主线、SOP 业务规则或无关运维模块。

## 权限

- 允许：生产服务器、数据库和第三方素材目录的只读查询；只输出聚合计数、脱敏摘要和校验值。
- 允许：读取业务目录中的非客户素材以计算身份；临时文件只能进入 ignored artifacts，任务结束清理，不记录签名 URL、凭证或素材字节。
- 允许：对虚构/匿名场景调用当前配置模型做 L2/L3，但必须使用隔离数据库、虚构身份、零发送和零生产写。
- 允许：在独立本地 MySQL 8 环境实现和验证未来可用于生产的同步工具。
- 禁止：任何生产 INSERT/UPDATE/DELETE/DDL、目录 apply、客户发送、第三方写接口、策略外发、业务开关修改、服务重启和部署。
- 禁止：把真实客户原文、客户身份、密钥、素材 URL、模型原始输出、完整日志或数据库 dump 写入 Git。

若只读审计发现正在发生持续客户误发、大面积素材消失、持续 5xx、身份串账或数据破坏，立即停止常规工作并以 P0/P1 形式报告产品负责人；这是事故边界，不是正常阶段请示。

## 技术要求

### 1. 现场影响审计

- 重新核验当前生产完整 SHA、dirty 状态、三个后端角色、前端、schema head、服务重启和错误状态。
- 确认素材治理运行代码是否已经生效，不能仅以迁移表存在推断。
- 统计所有当前可进入 V3 的素材：来源、媒体类型、业务角色、可靠 file ID、字节哈希、感知指纹、canonical/alias 状态、未知原因和跨来源重复组。
- 对比发布前后可获得的等长时间窗，聚合观察素材候选、成功身份、未知关闭、selected、`response_committed` 和结构附件比例。样本不足必须标明，不做强因果结论。
- 核查审计与管理日志没有把响应提交写成发送/送达，也没有泄露签名 URL。

### 2. 安全 MySQL 同步与回填准备

如现有工具无法安全面向生产 MySQL，补齐单一正式路径，至少满足：

- 默认只 dry-run；生产 apply 必须显式参数、明确数据库目标、计划文件和二次确认，测试环境不得误连生产。
- 计划绑定目录版本/checksum、schema head、生成时间和统计摘要；apply 前发现目录或数据库漂移必须拒绝。
- 可靠 file ID 优先；可取字节时使用精确哈希和版本化保守感知指纹；URL 不能单独冒充物理身份。
- 未知项保持 pending/disabled，不写成 verified，不猜测历史发送。
- apply 幂等、可中断和可恢复；有界批次、锁等待、超时、死锁重试和进度审计，不长时间锁住客户回复热路径。
- rollback 只能撤销该计划新增且尚未被业务占用的记录；已经 `response_committed` 的身份和别名不得自动释放。
- dry-run、apply 和 rollback 不输出凭证、签名 URL 或素材字节。

### 3. 隔离验证

- 用全新 MySQL 8/InnoDB/REPEATABLE-READ 验证迁移、重复迁移、旧 schema 兼容、目录同步、重复 apply、中断恢复、漂移拒绝、并发、回滚和进程重启。
- 使用生产规模或更大规模的合成目录与账本测量查询、计划和 apply 性能；已知素材请求热路径不得联网下载，身份 Join P95 目标不高于 100ms 且没有明显锁等待放大。
- 独立反例覆盖：同图跨来源、换域/签名、常见压缩缩放、相似但不同图、图片/视频隔离、不同接触档案、未知身份恢复和错误别名保护。
- 全量 L1、MySQL 专项、所有变更代码静态检查和必要前端正式构建通过。

### 4. L2/L3 行为矩阵

使用虚构客户和隔离运行环境，至少覆盖：

- 相关且 verified 的效果素材正常随短文字交付。
- 同一物理图跨来源、本轮重复和跨轮重复均只产生一次媒体交付。
- 未知身份不发附件，但文字回答非空、自然推进且不声称已发图。
- 身份后续补齐后，尚未占用且仍相关的素材可恢复候选。
- 活动、效果、跟进和卡点话术不能带出付款卡、预约完成、门店确认或写动作。
- 权威事实齐全的合法收款、预约和门店场景保持正常。
- 软拒绝和“给您留着/安排上”等软承接不被错误拦截。
- 跨企微、跨企业和跨外部联系人分别隔离。

最低门槛：结构解析 100%，硬安全 100%，未知身份附件误发 0，重复媒体结构 0，内容越权动作 0，未知身份文字链路非空 100%；合法动作如有任何回退必须判 No-Go 并定位原因。模型输出只保存在 ignored artifacts，AI 评分不冒充业务金标。

## Go / No-Go 标准

只有以下条件全部满足，最终才可建议后续执行生产回填：

- 当前生产没有素材治理引发的 P0/P1 客户影响；如果样本不足，只能给条件式 No-Go 或延长观察建议。
- 所有仍启用、可发送的目录素材都被明确分类为 verified 或显式 pending/disabled，不存在悄悄以 URL 当身份的未知项。
- 每个当前业务实际使用的关键素材角色/场景至少保留一个 verified 可用素材；任何场景因治理变为空都必须 No-Go，不能自行接受业务降级。
- 历史记录可证明的才回填；无法恢复的历史范围已量化，且不会被猜测成已发送。
- MySQL 同步计划可冻结、重入、恢复、漂移拒绝和受保护回滚，性能与锁范围达标。
- L1、MySQL、L2/L3 和必要前端构建达到上述门槛，没有未关闭 P0/P1。
- 最终计划明确生产执行顺序、备份、观察指标、停止阈值和恢复点，但本任务没有实际执行。

## 非目标

- 不重新调整自然度 Prompt、温度、意图、情绪、B 单或销售主线。
- 不治理每一张素材的业务内容是否正确、过期或适配项目；只输出需要业务确认的清单摘要。
- 不解决第三方实际发送/送达与失败补图闭环。
- 不恢复客户可见自动补答，不恢复策略 outbox 外发。
- 不执行生产回填、迁移、服务发布或真实客户链路测试。

## 交付要求

任务范围内自主完成全部取证、必要实现、修正和验证，不按阶段返回母窗口领取下一步。完成后：

- 在任务分支提交并推送干净候选；不得合入 main、不得部署。
- 长期产品/接口边界变化写入合同；动态现场事实只更新 current 文档；原始证据留在 ignored artifacts。
- 只提供一次最终五项摘要：`结论 / 改动 / 证据 / 风险 / 下一步`。
- 结论必须明确为生产回填 `Go`、`Conditional Go` 或 `No-Go`，并说明当前线上是否已受影响、是否有代码候选、哪些操作仍需单独生产授权。
