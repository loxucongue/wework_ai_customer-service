# SOP 业务判断失败有限恢复

- 目标：所有已拉取的第三方 SOP 任务最终进入发送 `30`、业务承接 `70` 或失败消费 `70`，不得因发送前接口异常无限占用。
- 非目标：不改变客户已开口、客户删除、人工接管三个业务终态；不改变主动发送调用后的幂等边界；不处理历史批量任务。
- base SHA：`f38983181b66bc62aef9656c4e048846b9ac7c5b`
- 生产基线：`ai-paths-unified-20260910-naturalness-dff19510` / `dff19510b97d8725e5aa6197e3328425590f6867`
- 涉及模块：第三方 SOP 确定性执行、失败告警、合同和测试。
- 不可破坏合同：主动发送前不得消费 `msgId`；主动发送已调用后不得重发；每个任务最多发送并消费一个内容组。
- 实现：发送前技术/资格查询失败最多观察三次；第三次仍失败则任务 `70`、不传 `messages`、失败策略回传、单次告警并释放顺序占用。
- 验证：运行 SOP 确定性执行、失败终态、告警合同及完整确定性测试。
- 发布：只允许 clean `main` 统一发布 control/reply/worker；发布后确认任务 `35058` 自动失败消费。
- 回滚：`ai-paths-unified-20260910-naturalness-dff19510`。

