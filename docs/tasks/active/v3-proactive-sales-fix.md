# V3 回复可靠性、性能与主动销售修复

- 目标：消除正常业务空回复和错误停推；效果场景有安全素材时同轮直接交付；软拒绝换价值角度继续销售；唯一门店同轮交付真实门店卡；按真实交付阶段推进；把非关键持久化移出 HTTP 返回路径。
- 非目标：不改变明确退订、人工接管、严重投诉/风险、健康风险和交易终态边界；不新增销售语义模型节点或公共回复接口。
- Base SHA：`f66803e6300b323368ca94164f1b6378561ba9c4`；开发期间已逐提交合并最新 `origin/main`。
- 生产基线：现场核验为 `main@3b717cd5429b84da1315f9d33bfbf61451d9189e`，release `ai-paths-unified-20260908-201613-3b717cd5`；其后 `main@940abdfd` 仅补 SOP 发布记录，已合入本分支。
- 分支：`codex/v3-proactive-sales-fix`。
- 独占范围：V3 Reply prompt/校验/素材与门店交付、`chat_runtime.py`、run 持久化与 Worker 收尾、运行日志观测及页面、相关测试和销售策略/系统架构合同。
- 不可破坏合同：V3 Reply 是唯一销售决策者；只有真实素材和门店 ID 才能交付；明确退订立即停止；不同企微的客户状态严格隔离；延时策略仍为 Shadow。
- 风险：过度强推、重复素材/门店卡、把普通异议误作授权、缺少真实素材时伪造交付、异步收尾积压或重复执行。
- 已完成：软拒绝与硬停止边界收敛；效果素材直接交付 Prompt、已采用素材的结构补齐和唯一门店卡结构校验；真实交付主线阶段摘要；主线未完成时不提前邀约；统一失败文本；回复核心单事务保存；可恢复的 Worker 异步收尾；`runs.created_at` 查询索引；日志页销售主线/素材/门店/暂停/兜底和分段耗时诊断。
- 合并审查：已合入生产先行的 `v3-reply-admission` 白名单合同。效果素材是否主动采用、门店详情后是否继续预约属于 Reply 模型质量，不新增 Python 语义拒绝门；已选素材完整交付、真实门店卡、付款、明确退订和预约完成态仍由代码硬校验。
- 验证：最终全量确定性测试 `599 passed`；变更范围 Ruff、前端 TypeScript、ESLint 和 37 路由生产构建通过。最终 120 条真实身份 DeepSeek 隔离评测：35 条实际 AI 请求全部通过，85 条人工接管正确跳过；策略核心覆盖、意图和情绪一致率均 100%，空回复、运行异常、无依据事实、安全失败和硬失败均为 0；19 条有话术候选样本采用 17 条（89.5%）；完整 P50/P95 为 10.66/14.62 秒，图后同步尾部 P95 为 0.162 秒。报告：服务器 ignored 目录 `/opt/ai-paths/evaluations/v3-proactive-120-4e96a354/`。
- 数据库：新增迁移 `20260908_01_add_runs_created_at_index.py`，只为 `runs.created_at` 增加索引，不新增业务表或字段。
- 发布：MySQL 全部 `aics_*` 表已备份到 `/opt/ai-paths/backups/pre-v3-proactive-20260908-204425/` 并通过 SHA-256 与 gzip 完整性校验；待合入 clean `main` 后执行迁移，control/reply/worker/frontend 使用同一 main SHA 对应版本上线。
- 回滚：恢复发布前后端与前端 release；新增查询索引可保留，不参与回复决策。
