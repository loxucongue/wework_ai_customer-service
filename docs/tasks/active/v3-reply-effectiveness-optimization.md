# V3 回复效果优化与对照验收

- 状态：evidence_and_dataset_freeze
- 分支：`codex/v3-reply-effectiveness-optimization`
- base：`93a99f5c50608e1513bd09272dcc3ac7600a20d5`
- 目标：回答当前问题、用相关论据解顾虑、自然积极推进；A 输入整理、B Prompt 精简、C 重复输出减负分别对照。
- 授权：只读生产抽样和脱敏，真实模型隔离评测，代码/测试及候选提交推送；不合入 main、不部署、不发送、不写生产业务表。
- 非目标：接待状态接口、SOP/主动唤醒、B 单执行规则、客户身份、外部协议、数据库 schema、模型供应商、额外节点/修复/拦截。
- Change contract：Reply 上下文和销售 Prompt 的质量优化；输出字段仅减少能无损派生的重复生成，不能由主要动作推断情绪/退订/交易状态。风险是事实丢失、交付/积极性回退和延迟上升；按固定事实与基线 SHA 分组对照，失败组不进入最终候选。
- ownership：`reply_synthesizer.py`、`reply_sales_prompt_v4.py`、本轮直接测试和独立效果评测工具、销售/评测合同。输出消费者先只读取证，不修改退款任务占用的 `reply_generation.py`，不改付款准入与结构修复。必要的超范围改动先停止该部分。
- 交接依据：用户批准本任务及范围交接；2026-09-14 原付款/性能工作区 clean、HEAD `6fb0d3f7`，处于自然流量监测。本轮接手 Prompt/上下文及效果测试，原任务保留交易/性能实现和监测职责。登记仅在候选分支，不擅自推 main。
- 生产基线：只读确认 release `ai-paths-unified-20260914-sop-priority-ff158e47`，control/reply/worker active；不代表已完成本轮质量验收。
- 数据：约50段调试＋30段独立隐藏对话，按完整客户会话分割并防近似重复；原始内容/标识/URL/模型输出只在受控 ignored artifacts，脱敏后清理原文副本；不以当前事实回填历史未知。
- 验收：固定 SHA/模型/temperature/目录/评分口径；每调试组至少3次；L1及隔离HTTP L3/重放/finalization；盲评新版胜>=60%、旧版胜<=15%，明确请求>=90%，多轮问题减半，交付/推进不回退，P50/P95不升、模型调用不增、发送/生产写入0。
- 业务验收：用户最终评分；开发只出对照包和初评。未盲评不得宣布业务胜出；无稳定改善组合则明确无胜出候选。
- 回滚：候选内按A/B/C独立提交保留/撤回，正式线上不变；不部署，无生产回滚动作。
- 已完成：最新main、dirty、其他ownership、生产服务只读核验，独立worktree。
- 待办：历史快照可恢复范围、样本冻结、消费者追踪、A/B/C实现与对照、全量测试、隐藏包、提交推送。
