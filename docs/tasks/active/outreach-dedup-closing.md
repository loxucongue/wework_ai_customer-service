# 沉默唤醒去重与成交承接

- 目标：消除同一客户计划内和跨计划的重复话术；跟进序列不重复执行已完成节点和已用话术；客户卡点被有限次数承接后，回到尚未完成主线或执行一个有事实依据的预约/预约金意向动作。
- 非目标：不恢复独立延时逼单真实发送；不绕过客户回复、人工接管、删除关系、订单终态、支付和预约安全门；不修改 V3 即时回复业务链。
- Base SHA：创建分支时为 `05723ce3ab88a629f659e6f0caf51bfac8c7f6eb`，首次提交后已 rebase 到 `497260d95083080acbc0510725de5c1a0e7555ca`。
- 生产基线：2026-09-08 21:45 +08:00 现场核验 control/reply/worker 统一运行 `05723ce3ab88a629f659e6f0caf51bfac8c7f6eb`，当前 release `ai-paths-unified-20260908-v3-proactive-05723ce3`；previous 为 `ai-paths-unified-20260908-201613-3b717cd5`。
- 分支：`codex/outreach-dedup-closing`。
- 涉及模块：`ai_paths/app/services/outreach/`、outreach repository、沉默唤醒 prompt 与相关确定性测试。
- 不可破坏合同：客户边界为 `corp_id + wechat + external_userid/customer_id`；模型判断业务语义和成交节奏，代码只做事实输入、目录约束、进度、幂等和重复安全门；明确退订立即停止；没有权威支付/预约条件时不得伪造完成或发送付款卡。
- 风险：去重过严导致合法补充被阻断；错误继承其他企微进度；成交承接过早；跳过平台序列节点后时间顺序变化；并发任务读取到过期历史。
- 验证：确定性覆盖计划内候选排重、跨计划已用话术、所有步骤文本相似校验、节点压缩、成交承接来源、身份隔离、无新价值安全终止；再执行相关测试、全仓测试和 Ruff。
- 发布：本任务只在 clean `main` 合并并完成发布清单后上线；发布前审计现有 pending/checking，不直接恢复旧重复任务。
- 回滚：回滚到本任务合并前 clean main release；本任务不回写历史已发送消息。

## 当前进度

- 已完成：生产数据与生产代码只读取证；确认历史固定 SOP 与当前跟进序列是两类重复来源；确认真实 9 节点距离序列存在成对重复节点且缺少预约/预约金决策出口。
- 已完成：计划内节点压缩、跨计划真实发送进度、话术与价值维度排重、最终成交承接、预约金事实门禁、全步骤文本相似校验和日志字段。
- 已修复审查问题：无卡点主线候选会按同一销售接触边界的近 30 天真实发送记录确定性排除；主线全部完成后进入独立单步成交互动模式；任务物化不会继承旧 SOP 消息或素材；最终回主线任务按实际采用来源记录进度。
- 验证证据：相关回归 39 条、全仓 613 条测试、变更范围 Ruff、全后端编译通过。生产只读审计发现 2 个旧逻辑计划仍有 9 个非终态任务，其中已完成步骤具有平台消息 ID，剩余任务无消息 ID；发布前必须停止 worker 后按精确计划 ID 取消，不恢复或补发。
- 待办：真实模型单节点效果测试、旧任务处置、合并、发布及发布后健康与只读数据核验。
