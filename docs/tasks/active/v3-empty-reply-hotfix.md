# V3 空回复 P0 热修

- 目标：消除接待状态接口异常导致的 HTTP 200 空回复，前移同消息幂等，限制背景上下文阻塞，并统一最终兜底为“您稍等一下”。
- 非目标：不修改销售语义、跟进策略、话术知识和主动触达业务规则。
- Base SHA：`6244e14aa5cc1038d4c71ff945f83fcbe696711e`
- 生产基线：现场 release `ai-paths-unified-20260908-152743-9e0f24c2`；发布前再次核验。
- 涉及模块：V3 入口、平台消息协调、接待状态门禁、背景上下文超时、确定性兜底。
- 不可破坏合同：人工模式不自动回复；明确退订不营销；V3 唯一回复入口；相同消息 ID 幂等。
- 风险：状态查询不可用时误回复人工会话；重复请求并发；数据库阻塞；上游提前断开。
- 验证：重复消息并发、状态超时/502、人工模式、背景上下文超时、兜底文案、V3 路由与生产健康检查。
- 回滚：恢复发布前记录的 release；不涉及数据库迁移。
- 已完成：平台协议过滤后，以 `corp_id + wechat + external_userid + msgid` 在单次任务内完成接待状态查询；状态未知返回唯一中性兜底，明确人工仍不回复；同步数据库前后段移出事件循环；客户记忆读取增加 8 秒等待上限。
- 测试证据：V3 专项 `194 passed`；全仓 `515 passed`；Python 编译与 `git diff --check` 通过。
