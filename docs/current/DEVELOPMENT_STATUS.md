# 当前开发进度

- status: active-production-development
- code_baseline: `production@3fc85db46a17662aa47e0903297eb515de13ec43`（协议消息快速忽略与沉默扫描调度收尾已上线）
- production_verified_at: `2026-09-08T12:39:17+08:00`
- source_of_truth: 当前 main、生产现场核验与对应合同

本页给新窗口提供统一的产品进度。一次性测试报告、客户原文和原始模型输出不在这里保存；详细实现回查 Git 和 [已完成任务索引](../tasks/history/INDEX.md)。

## 当前能力总览

| 板块 | 当前状态 | 已实现效果 | 主要机制 | 仍需完成 |
| --- | --- | --- | --- | --- |
| V3 客户回复 | 已上线 | 唯一产品回复入口；连续消息合并后只返回最新结果；简单问题优先单条真人微信；已有权威价格和相关效果素材直接交付，不用“要不要发”制造无效对话；平台固定开场和撤回协议消息直接空回复并保留轻量审计 | 协议过滤在 AI/人工状态、语音、消息协调和模型链之前执行；普通消息由 Router `deepseek-v4-flash`，最终 Reply/修复/重试 `deepseek-chat`，代码做事实、媒体 ID、去重和安全校验 | 持续检查真人感、5xx、素材过度发送、协议过滤命中率和完整 HTTP 耗时 |
| 意图识别与路由 | 已上线观察 | Reply 输出 7 类最终主意图和次要信号；Router 只负责检索与工具规划 | 同一 Reply 调用统一判断，BI 记录置信度和跨轮变化 | 仍缺 400 条业务人工确认金标，不能把结构覆盖当准确率 |
| 情绪分析 | 已上线观察 | 8 类情绪调节表达压力；爆粗、讲价和短句不单独判愤怒 | Prompt 最低证据/反例 + 代码派生流程动作 + 安全边界 | 用业务金标评估愤怒、急躁、防御和普通语气的混淆矩阵 |
| 卡点、跟进序列与话术 | 已上线 | 同类型卡点话术不会再被动作/tag 硬过滤成零；活跃卡点存在直接相关且安全的候选时必须采用一个主话术并记录真实 ID；采用话术附带的安全未发送媒体可随回复直接交付 | 外部 Follow Knowledge + 确定性 Top-K；序列节奏与话术解卡双轨采用；模型选话术、代码校验并补齐该话术自身媒体 | 监控“有安全候选但未采用”、媒体重复/过量和目录异常；不相关、冲突或压力不合适时不能为采用率强用 |
| B 单规则/策略/话术 | 已上线观察 | Router 召回资格与策略，Reply 统一选择进入、推进、暂停或完成 | `external_then_local`；临时真人成交版目录 9 条规则、16 套策略、37 个节点、42 条话术 | 业务导入第三方后核对版本、稳定 ID、checksum 和节点话术外键 |
| 门店工作流 | 已上线 | 权威匹配、缺位置追问、多门店完整交付；门店卡交付与区域推荐依据分开保留，同城终态距离异议不再无效追问、重查或重发卡 | 门店解析模型 + 只读门店事实 + 推荐终态 + 结构消息逐卡幂等 | 线上继续核对新城市重匹配、同城列表文字化和距离话术采用 |
| 预约前推进 | 已上线 | 已确认门店且客户问停车/营业时间等细节时，未预约且无新卡点则答清并连接预约登记/预约金，不主动发付款卡 | Reply Prompt 的行动信号合同 + 权威预约金事实 + 结构消息校验 | 监控无依据“保留名额”、已预约仍重复邀约和无目的时间提问 |
| 策略 BI | 已实现并上线 | 记录意图、情绪、卡点、规则、序列、话术、采用、送达和后续窗口结果 | `v3_strategy_usage_events`、`v3_strategy_outcome_events` 和管理聚合接口 | 平台订单异步归因开关未在本次现场核验中确认启用；成交只能做窗口相关性 |
| AI 回复日志页 | 已上线 | 默认看客户原话、最终回复、意图/情绪/卡点、候选/采用/发送和节点状态；原始数据按需下钻 | runs + usage event 的只读聚合，节点详情延迟加载并脱敏 | 历史未留存字段只能显示“未记录”，不能补成零或用当前目录回填 |
| 沉默客户唤醒 | 已上线 | 不限加微时间，客户沉默 1 分钟进入候选；有卡点按外部跟进序列完整节点生成任务，无卡点按未完成主线生成；取消固定两步和每日 2 计划/4 任务；人工或状态未知不处理 | 独立 DeepSeek 选择卡点/序列和节点话术 + Follow Knowledge 渐进候选 + 夜间顺延/活跃 40 分钟压缩 + 计划前/发送前平台 AI 状态检查；扫描目标间隔 15 秒，长轮后至少等待 5 秒，异常按 15/30/60 秒退避且单实例不重叠 | 1 分钟是候选资格，不是准点发送 SLA；现场单轮约 32～34 秒。最长 11 节点夜间压缩的触达密度是业务已接受风险，必须监控回复、退订和逐次阻断 |
| 沉默唤醒日志页 | 已上线 | 一屏看懂客户需要、卡点/主线模式、真实序列、动态节点数、话术候选/采用、调度和发送结果；原始输入输出按节点折叠查看 | 运行列表 `business_summary` + 详情 `observability_view`，只从当时快照、真实目录 ID 和任务事件派生，不补写历史事实 | 历史记录缺少新字段时显示“未记录”；候选、采用和发送不得混为一类 |
| 主动唤醒 BI 看板 | 已上线 | 展示六步漏斗、趋势、阻断原因、账号分布和当前队列；队列可下钻客户、外部联系人、会话、平台客户、企业、接待人员、企微、运行、计划、任务及平台消息 ID，并逐项复制 | 复用 `first_day_outreach_runs`、计划、任务、事件、会话和客户入站消息做同一销售接触边界内的只读聚合；前端并行读取 Worker 健康 | 历史未留存的会话或任务 ID 显示“未记录/尚未生成任务”，不得伪造；继续观察 MySQL 冷查询与跨日数据量下的响应时间 |
| 客户身份治理 | 已上线 | 平台客户、外部联系人、平台接待人员、企微和加微关系 ID 分开传递、展示和查询，不再互相补位 | 入口合同校验 + `customer_identity_links` 映射/冲突审计 + 销售接触范围 `corp_id + wechat + external_userid` | 历史表仍有旧式混用记录，只读兼容但不自动当作权威映射；后续按平台核验结果逐步治理 |
| 延时 B 单/四大区跟进 | Shadow | 可以记录建议和计划，不产生真实客户触达 | policy `silent_tasks_mode=shadow` 与发送适配器阻断 | 未经独立发送安全验收不得启用自动发送 |
| 第三方 SOP | 运行中并有独立管理 BI | “监控”下的 `/analytics/sop` 区分平台任务、本地任务、客户、真实发送、无需发送、异常和未完成；任务证据与底层事件分别下钻 | 独立 worker、幂等状态、终态和策略数据回传合同、MySQL 只读聚合、worker 实时健康 | 当前队列无积压；1 条模型 503 可恢复异常需继续观察，策略数据 outbox 外发关闭问题另行处理 |

## 当前统一目标

1. 保持正常客户“答清问题 → 给出相关价值 → 推进预约金”的主线，不因情绪或规则过度保守而停止销售。
2. 有卡点时确保获得同类跟进序列/话术候选；存在直接相关、无事实冲突且压力合适的安全候选时 Reply 必须采用一个主话术，不符合这些条件时不得为采用率强用。
3. B 单只在客户授权、事实齐全且卡点已处理时进入；新卡点、投诉、高风险和明确退订优先。
4. 不增加重复销售模型调用；优化优先从上下文、Prompt、候选质量和事实输入解决。
5. 用日志和 BI 解释“识别了什么、召回了什么、采用了什么、实际发了什么、客户后来怎样”，但不宣称强因果。

## 当前优先级

| 优先级 | 工作 | 完成定义 |
| --- | --- | --- |
| P0 | 持续 L4 生产生命周期观察 | 当前已核对同一 main SHA、真实 HTTP/Nginx、结构消息和服务角色；继续积累生产 P50/P95/P99 与 MySQL 连接稳定性 |
| P0 | 线上回复质量与延迟观察 | 重复门店卡、无目的推进、审计腔、兜底和端到端 P95 可从日志定位并保持在接受范围 |
| P1 | 400 条业务人工金标 | 分别形成意图、情绪、压力方向、卡点、序列/话术适配和 B 单动作的正式验收集 |
| P1 | 策略数据 outbox 专项 | 查明 16 条 dead、13 条 pending 的来源，明确是否重放；恢复外发前保证幂等和可回滚 |
| P1 | 第三方 B 单目录切换验收 | 外部规则和策略同时非空、版本和 ID 关系可信，且本轮来源可审计 |
| P2 | 订单结果归因完整性 | 明确平台只读订单归因开关、查询成功率和未知状态，避免把未知写成未成交 |

## 评测口径纠正

历史 120/400 条 DeepSeek 隔离评测能够证明模型节点在冻结输入下的结构和语义表现，但没有完整模拟生产的结构化已发送消息、真实持久化数据量、图执行后的写入尾部以及公网 HTTP 生命周期。因此：

- 既往 `93.33%` 是策略结构覆盖基线，不是完整意图/情绪/B 单业务准确率。
- 既往高通过率不能证明不会重复发送门店卡，也不能证明生产端到端延迟达标。
- AI 评审只算初评；业务推进目标缺失、结构消息重复和硬安全边界必须由确定性规则直接判失败。
- 后续只有满足 [V3 Reply 质量与全链路评测规范](../standards/V3_REPLY_EVALUATION.md) 才能称为“全场景/全链路通过”。

当前版本在 120 条真实身份只读 L3 中运行错误为 0、DeepSeek AI 初评 99.2%、真人表达 100%，完整生命周期 P50/P95 为 8.74/13.93 秒，安全、无依据事实、生产发送和关键表写入均为 0。该批唯一硬失败是城市列表夹带一张历史已送达门店卡；加入逐卡幂等后已对原样本单独重放通过，并保持长沙 4 家门店事实完整。指定距离异议日志也已采用真实序列/话术 ID，且无细地址追问、重查或重复卡。随后针对“已采用话术但效果图未交付”完成 2 条指定场景和 20 条真实身份 DeepSeek 只读复测：指定场景 2/2 通过，20 条 AI 初评/真人表达均为 95%，许可式素材追问为 0，策略适用样本序列/话术采用 6/6，P50/P95 为 7.54/11.92 秒。唯一失败是“客户自述已付预约金但缺少权威预约事实”进入现有安全兜底，需另行优化；以上仍不是 400 条业务人工金标。

生产现场详情见 [生产状态](PRODUCTION_STATE.md)，当前风险见 [已知问题](KNOWN_ISSUES.md)。
