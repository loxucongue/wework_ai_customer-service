# 已知问题

- status: active
- source_of_truth: 当前代码与发布任务现场核验

1. AI 销售策略已在生产启用，延时逼单和四大区跟进继续保持 shadow，不允许因目录存在而自动发送。首版 DeepSeek 隔离评测的策略结构覆盖为 93.33%，由产品负责人接受上线；仍缺 400 条业务人工金标，线上看板必须区分结构覆盖、分类准确率和成交相关性。
2. 当前生产 B 单目录采用 `external_then_local`，已加载业务真人成交版临时目录 `2026-09-05.business-workbook-real-deal-v1`：9 条规则、16 套策略、37 个节点和 42 条话术。业务接口返回正常且非空目录后会自动优先外部；切换时仍需核验来源、checksum、稳定 ID 和节点话术类型关联。
3. 历史测试套件与评测资产不作为长期事实来源。后续功能变更必须由对应任务建立最小、隔离验证，并把运行产物保存在 ignored `artifacts/`。
4. `chat_runtime.py`、`reply_nodes.py`、`reply_validation.py` 和 `sop_platform_task_service.py` 仍是超大核心模块。新功能必须落到已有职责边界内；没有明确重构任务和回归证据时，不得顺手重写或横向搬动这些模块。
5. 门店 4～6 家交付、超过 6 家文字清单、详情目的保留及同城终态重复卡修复已在 `8f911bea` 发布。当前城市列表若混有历史已送达卡，整轮改用完整文字清单，避免重发卡或把 4 家误报成 3 家；线上仍需监控候选完整度、恢复率和新城市重匹配。
6. `aics_strategy_data_outbox` 在 2026-09-07 19:49 现场核验为 16 条 dead、29 条 pending，且 delivery 当前关闭；这不是本次沉默唤醒发布产生，但恢复策略数据外发前必须单独审计并处理，不能直接批量重放。
7. DeepSeek 在仅看到客户说“这家门店可以”、但没有门店工具或历史真实门店卡证据时，仍可能在候选层提名“已接受门店”规则。该候选不能授权门店或预约事实，最终 Reply/结构消息仍受权威门店事实校验；后续第三方规则字段若能提供结构化事实前置项，应优先在目录合同中解决，而不是追加关键词分支。
8. 新 L3 评测已使用一次性 SQLite、完整 `ChatRuntime`、结构化已送达历史、策略事件和响应序列化，并以生产只读身份执行 120 条样本；但真实样本只包含 3 条已发门店卡、1 条“已发卡后问门店详情”，状态稀有组合仍必须依靠确定性矩阵补齐，不能宣称业务全场景金标通过。
9. 新 L3 已把模型图和图后尾部分开计时，120 条图后持久化与返回 P95 为 56ms，说明旧 69 秒问题不在当前临时库尾部路径；这仍不能代表生产 MySQL、Nginx 和网络负载。发布后的 L4 必须继续统计完整 HTTP P50/P95/P99，不能只看模型图。
10. 生产在 2026-09-07 19:49 的即时回滚指针为：后端和 Reply 均指向上一 clean release `ai-paths-unified-20260907-185509-25cea55f`；前端仍使用 `frontend-20260907-152202-b4dfc184`。后续发布仍须现场重验，不能永久依赖本条快照。
11. 生产 MySQL/RDS 连接在本次发布前后均出现间歇性 connect/read timeout；发布后曾使沉默唤醒扫描单轮失败，但 worker 未退出、SOP 队列和 pending 均为 0、最近轮询错误为空。该问题与门店修复无代码关联，回滚不能消除；需单独检查 RDS 网络、连接池和超时告警，期间未知状态继续失败关闭，不能误发客户消息。
12. 生产根分区在本次发布后为 77%、剩余约 8.9 GB；已脱离紧急水位，但仍需按发布保留规范维护历史 release。当前 release、已验证回滚 release 和环境备份不得删除，未完成可恢复归档前不直接批量删除历史目录。
13. 新一轮 20 条真实身份只读复测中，有 1 条客户自述“已付预约金”，但本轮没有权威预约/支付确认；现有事实校验正确阻断了无依据确认，却退成通用安全兜底，客户体验不足。后续应在不把客户自述当权威交易事实的前提下，生成“已收到您的说明，我先帮您核对订单/预约记录”的定向回复，不得直接确认已支付或已预约。
14. 沉默唤醒的 1 分钟配置只表示客户进入候选资格，不保证恰好第 1 分钟发送。计划需完成候选扫描、平台 AI 状态、会话、订单和 DeepSeek 决策，RDS 抖动还会增加排队时间。2026-09-07 已把逐客户指纹查询改为单次有界快照，生产 1038 个候选扫描约 33.5～35.3 秒且错误 0；不可达会话不再无限重试，临时 AI 状态异常最多重试 3 次。吞吐仍不能通过跳过 AI/人工复核或无界并发发送解决。
15. 沉默计划已经支持图片和视频两类真实素材上下文及任务物化，但 2026-09-07 现场目录实际为 68 张图片、0 个视频。代码不能凭空补视频；需要视频效果时必须先由业务在现有 SOP/卡点素材配置中提供真实可发送视频。日志页会如实显示类型数量和本轮是否实际附加。
16. 2026-09-07 第三方目录共有 92 条序列、544 个节点、522 条话术和 28 种实际动作码；只有 137 个节点存在“同卡点且同动作”候选，其余 407 个节点依赖同卡点放宽或全目录语义候选。隔离测试已做到 544/544 节点有候选、28/28 动作样本被 DeepSeek 采用，但这不等于每个真实客户语境都应强制采用。线上必须监控匹配范围、拒绝原因和客户结果，不能只追求采用率。
17. **P1 生产内存与发布构建优化待办**：2026-09-07 现场核验服务器只有约 1.8 GiB 内存，近 7 天发生 13 次 OOM，其中 12 次杀死 `/usr/bin/node` 的 `MainThread`，单次匿名内存约 660～960 MiB；发生时间与前端 release 构建高度重合，当前前端运行态约 83～123 MiB，首要问题是生产机执行 `pnpm install`/`next build` 的瞬时峰值，而不是已证明的运行态持续泄漏。后续任务应把前端构建迁出生产机，只上传已验证产物；修正 release 清理范围以覆盖实际的 `/opt/ai-paths-frontend/releases` 和 `/opt/ai-paths-v3/releases`，保护当前与已验证回滚版本并收敛到最近 3 个；确认无调用后再停用当前约 123 MiB、两个月网络统计为 0 B 的 SearXNG；为构建和前端运行配置分离的内存上限及 OOM/MemAvailable/swap 告警。当前 backend/frontend/V3 release 数分别为 13/13/24，根盘约 76%；不得用手工 `drop_caches` 代替治理。验收至少包括连续 7 天无新增 OOM、release 数符合保留合同、核心服务健康且常态 `MemAvailable` 不低于 300 MiB。
18. 2026-09-07 身份质量接口首次上线时，历史表共发现 4,807 行旧式 `customer_id` 混用或与 `external_userid` 相同的记录；其中部分来自已退役兼容写法。新版只读兼容这些历史行，不把它们当作权威平台客户映射，也不会自动批量覆盖。后续治理必须通过平台客户资料查询形成已验证映射；未知或冲突状态失败关闭，不能按前缀猜测后直接改库。
19. 2026-09-08 已修复沉默唤醒计划生成的两个代码缺口：后置回读/审计失败不再反向破坏已提交计划，自动批准 draft 会在重复指纹拦截前恢复；过期节点排期保留平台相对间隔。仍需持续观察 RDS/SSH 抖动：本次发布和验证过程中出现多次 SSH reset/timeout，历史 MySQL `InterfaceError (0, '')` 也会拉长 worker 扫描和 V3 图后持久化，不能把 1 分钟沉默阈值理解成 1 分钟必达发送。
