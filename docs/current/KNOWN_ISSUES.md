# 已知问题

- status: active
- owner: project
- last_verified: 2026-09-01 Asia/Shanghai
- source_of_truth: 当前迁移工作区、测试结果；生产事项仍需现场核验

## P0：生产版本尚未随本次迁移核验

仓库 `main` 已统一 V3、SOP、回调、存储和 worker 代码线，但本次 AI 策略迁移尚未部署。正式发布前必须重新确认生产 release、数据库后端、worker/outbox 单消费者和回滚点。

## P1：AI 策略效果尚未完成人工验收

- 400 条评测数据是自动生成的候选，不是人工金标。
- 策略目录和延时任务保持 shadow；不能据旧开发窗口的少量模型结果宣称准确率或转化提升。
- 真实启用前仍需单节点模型效果测试和全链路在线测试。

## P2：生产尚未应用本次代码治理

本次治理删除了应用内旧路由、旧 Planner、通用 Outreach 工作台和旧沉默扫描，增加了
`control/reply/worker` 角色隔离，并重组了 SOP 与 Outreach 实现。仓库验证通过不表示线上已经生效；
正式发布仍需现场更新并核验三个 systemd 角色配置。

## P2：测试封装与异常处理债务

当前 152 处测试直接导入私有符号；`except Exception`、静默吞异常和启发式 fail-open 的统计与口径见质量门禁文档。现有确定性业务回归仍保留，后续修改相关域时必须优先改成公开合同，禁止新增私有导入。

## P3：核心文件仍然偏大

Outreach 和第三方 SOP 的公开入口已经阶段化，不再由单个巨型方法承担全部流程；但历史辅助函数仍集中在较大的模块中。新开发必须把新能力放入现有明确阶段，禁止再次把跨阶段逻辑塞回编排入口。只有在修改对应辅助域时再做就地迁移，不为追求文件行数引入抽象层。
