# 已知问题

- status: active
- source_of_truth: 当前代码与发布任务现场核验

1. AI 销售策略已在生产启用，延时逼单和四大区跟进继续保持 shadow，不允许因目录存在而自动发送。首版 DeepSeek 隔离评测的策略结构覆盖为 93.33%，由产品负责人接受上线；仍缺 400 条业务人工金标，线上看板必须区分结构覆盖、分类准确率和成交相关性。
2. 当前生产 B 单目录采用 `external_then_local`，已加载业务真人成交版临时目录 `2026-09-05.business-workbook-real-deal-v1`：9 条规则、16 套策略、37 个节点和 42 条话术。业务接口返回正常且非空目录后会自动优先外部；切换时仍需核验来源、checksum、稳定 ID 和节点话术类型关联。
3. 历史测试套件与评测资产不作为长期事实来源。后续功能变更必须由对应任务建立最小、隔离验证，并把运行产物保存在 ignored `artifacts/`。
4. `chat_runtime.py`、`reply_nodes.py`、`reply_validation.py` 和 `sop_platform_task_service.py` 仍是超大核心模块。新功能必须落到已有职责边界内；没有明确重构任务和回归证据时，不得顺手重写或横向搬动这些模块。
5. 2026-09-05 专项已修复 V3 同城多门店候选数与最多 3 张门店卡合同冲突，以及具体广告/案例无事实时误确认的问题；这两类确定性复测已通过。生产仍需重点监控事实校验失败、兜底率、退订误推进和无权威事实项目承诺，不得通过关闭事实校验提升表面通过率。
6. `aics_strategy_data_outbox` 发布前已有 16 条 dead，且 delivery 当前关闭；这不是本次 Reply 发布产生，但恢复策略数据外发前必须单独审计并处理。
7. DeepSeek 在仅看到客户说“这家门店可以”、但没有门店工具或历史真实门店卡证据时，仍可能在候选层提名“已接受门店”规则。该候选不能授权门店或预约事实，最终 Reply/结构消息仍受权威门店事实校验；后续第三方规则字段若能提供结构化事实前置项，应优先在目录合同中解决，而不是追加关键词分支。
