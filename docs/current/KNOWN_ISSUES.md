# 已知问题

- status: active
- owner: product / engineering / operations
- verified_at: `2026-09-11T13:27:16+08:00`
- source_of_truth: 当前代码、最新生产现场与未关闭验收项

本页只保留尚未闭环的问题。已修复经过不在这里重复，查询 Git 和
[任务历史](../tasks/history/INDEX.md)。

| 优先级 | 问题 | 当前影响/控制 | 关闭条件 |
| --- | --- | --- | --- |
| P0 | 无当前 P0 | — | 发现客户误发、数据破坏、持续 5xx 或明确安全越界时立即登记并按发布手册处理 |
| P1 | 缺少正式业务金标 | 历史 DeepSeek 自评只能说明候选质量，不能证明意图、情绪、B 单和真人表达已达业务准确率 | 400 条匿名真实样本经业务逐条确认，并按单类与安全边界出正式报告 |
| P1 | B 单仍可能使用临时本地目录 | `external_then_local` 在外部未配置、异常或空目录时使用 provisional JSON；目录事实可能与未来平台正式版不同 | 外部规则、策略、节点话术非空，稳定 ID/版本/checksum 与关联验收通过，生产连续命中正常 |
| P1 | 策略 outbox 有历史积压且外发关闭 | 最近现场为 pending 58、dead 16；直接重放可能重复回传 | 逐类审计幂等键和上游状态，制定可回滚重放清单后再启用 delivery |
| P1 | RDS 网络长尾 | 应用私网但 MySQL 主机名仍解析到公网；冷连接和网络抖动可拉长无模型路径及 Worker 扫描 | 独立完成同 VPC 内网切换、备份与回滚演练，生产 P95/P99 达标且无连接错误 |
| P1 | 生产构建内存/OOM 风险 | 小内存主机历史 OOM 多与前端构建峰值重合；运行时未证明持续泄漏 | 前端改为站外构建后上传产物，release 按合同收敛，连续 7 天无新 OOM 且内存水位稳定 |
| P1 | 客户可见自动补答关闭 | 代码具备有限恢复，但恢复后素材/门店记忆、finalizer 异常和真实回调尚未独立验收 | 三类发送安全测试、幂等/取消测试和生产受控验证全部通过后单独启用 |
| P1 | 聚合平台重复消费不受本仓库控制 | AI 已提供稳定响应和消息 ID，但下游若忽略仍可能重复发给客户 | 对照 AI run、dispatch、聚合平台发送与送达回调，证明同一 `client_message_id` 只发送一次 |
| P2 | 素材业务质量与超限项依赖目录 | 系统能防伪造和重复，不能判断某素材是否项目错误、过期或业务表达不当；当前 6 个超过 6 MiB 的引用明确保持 pending/disabled，但 13 个角色均仍有 verified 素材 | 业务提供过滤后的目录版本/checksum，处理或替换 6 个超限项，并完成素材抽检和线上采用复盘 |
| P2 | 素材历史占用只能部分恢复 | 历史 2,245 个媒体结构记录中只按精确证据恢复 22 个唯一 contact/canonical 占用；2,216 条与当前目录 URL 无法匹配，另有 4 条缺完整 contact 边界，禁止猜测生成 claim，因此仍有少量旧图重复风险 | 第三方提供稳定 file ID、历史别名或字节证据并按 contact 边界审计回填；没有证据的历史保持未知 |
| P2 | 门店营业时间和到店指引不完整 | 缺字段时只能如实说明并生成内部待补事实，不能给具体时间/楼层 | 第三方补齐权威字段并完成唯一门店、未付/已付边界回归 |
| P2 | 稀有生命周期状态覆盖不足 | 真实样本中的“已发结构卡＋阶段状态＋当前异议”等组合仍稀疏 | 建立状态组合矩阵并纳入每次 L3/L4 验收，结构消息也进入评审输入 |
| P2 | 沉默 1 分钟不等于准时发送 | 仍需扫描、模型、AI 模式和发送前门禁；RDS/Worker 负载会延迟 | 看板持续展示资格时间、计划时间、实际发送和阻断阶段，并定义产品可接受 SLA |
| P2 | 30/90 天主动唤醒明细查询较慢 | 默认 7 天热查询已优化，长窗口只适合审计 | 增加与事实口径一致的汇总索引或异步报表，不能靠放宽身份边界提速 |
| P2 | 历史客户身份混用 | 旧数据存在平台客户 ID 与外部联系人 ID 混写；新代码失败关闭但不会自动批量改写 | 通过平台资料形成已验证映射，分批审计迁移并保留冲突记录 |
| P2 | 协议消息轻量审计存在极小丢失窗 | 响应先返回、审计随后落库；进程被强杀时只能由 Nginx 取证 | 若业务要求绝不丢失，改为本地可靠 outbox；不能把远程 RDS 写回客户响应前 |
| P2 | 核心模块仍偏大 | `chat_runtime.py`、`reply_nodes.py`、`reply_validation.py` 和 SOP 执行模块维护成本高 | 仅在独立重构任务中按职责拆分并以全量回归证明无行为改变 |
| P2 | 表情和真人感尚无业务验收闭环 | 当前 Prompt 允许普通销售轮最多一个轻表情，但不是强制；确定性测试不能证明自然 | 销售主管按场景人工复核至少 50 条，确认表情频率、短消息和推进强度后再调整 Prompt |
| P3 | 门店目的地解析仍允许 GPT fallback | 最终 Reply 是 DeepSeek-only，但门店事实解析 tier 的代码默认仍含 GPT 候选 | 产品若要求全链 DeepSeek，显式清空该 tier fallback，完成门店消歧准确率和故障率对照后发布 |

任何问题只有同时具备代码/配置证据、确定性测试和所需的生产或业务验收，才能从本页删除。
