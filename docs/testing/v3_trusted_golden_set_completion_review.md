# V3 可信黄金集完成度审查

审查基线：`codex/v3-model-led-sales-brain@eceed97bc0c6eec6f026a86a5f1a6001b62cd509`

## 结论

可信黄金集的“建设与业务审核”阶段已经完成，可以作为离线评测标准使用。Critic 校准、Critic 保留集验收和 V3 模型正式效果评测尚未执行，因此当前不能据此宣称 V3 已达到 90% 语义通过率或已经具备上线条件。

## 已完成

- 50 条多轮轨迹均已由最终业务审核人批准。
- 来源构成为 30 条脱敏真实回放、10 条现有夹具、10 条边界或对抗场景。
- 预约卡点经验来源审计覆盖 104 条原始记录，其中 34 条含禁止内容、79 条素材待审核；原始成品话术不作为黄金答案。
- 每条轨迹均包含客户目标、必答点、可接受动作、Gate 素材要求、最终交付要求、禁止动作、禁止事实和 1–2 个非唯一参考示例。
- 已按固定规则划分为 15 条 Critic 校准集和 35 条保留评测集。
- `golden_v3_004` 已保留“完成线上活动登记后再提供免费皮肤检测”的前提。
- `golden_v3_005`、`golden_v3_046` 已统一为明确拒绝后停止当前营销推进，且不包含未经授权的清洁小气泡赠品。
- 正式 fixture 明确为离线评测专用，运行时 Prompt、Gate、Reply 和事实审计禁止导入。
- 评分禁止使用参考回复文本相似度，避免把模型训练成场景匹配器。

## 本次代码审查

- V3 Reply 的动态权威事实优先级高于历史助手文本，避免旧回复覆盖当前活动、退款、门店和支付事实。
- 新增的回复重复度只记录可重建数值，标记为 `measurement_only`，不参与路由、拦截、修复或客户可见文本改写。
- V3 `evidence_join` 路径不执行旧版节奏、精准问答主线和场景语义检查；这些检查仍只服务未迁移的旧链路。
- Gate 使用并行内容召回合同，不使用旧 `selected_scene_id` 场景选择 Prompt。
- 黄金集及 Critic 未进入线上回复链路，不会改变当前客户回复。

## 尚未完成

- 使用 15 条校准集校准离线 Critic，并记录人工标准与 Critic 的一致性。
- 在 35 条保留集上验证 Critic 精确率不低于 90%、误报率不高于 10%。
- 运行 V3 Gate、Reply 和最终交付评测，分别计算 Gate Recall、False Nomination、Reply Adoption、False Adoption 和 Delivery Completion。
- 验证首次询价错误发卡率、成熟付款及时发卡率、软拒绝错误暂停率、明确拒绝错误挽回率等效果指标。
- 在上述结果通过并经人工复核前，不得把本次提交描述为 V3 效果验收完成。

## 使用边界

黄金集是考试标准，不是线上话术库。后续失败应优先定位 Gate 召回、Reply 判断、素材交付或事实输入，不得把单条黄金样本复制进 Prompt，也不得新增 Python 客户关键词分支。
